<UserControl x:Class="WpfExample.App.Mvvm.Views.BodyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfExample.App.Mvvm.Views"
             xmlns:viewModels="clr-namespace:WpfExample.App.Mvvm.ViewModels"
             xmlns:converts="clr-namespace:WpfExample.App.Converters"
             d:DataContext="{d:DesignInstance viewModels:BodyViewModel}"            
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type viewModels:HomeViewModel}">
            <local:HomeView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:ProductsViewModel}">
            <local:ProductsView />
        </DataTemplate>
    </UserControl.Resources>
    
    <Border CornerRadius="0,0,20,20" 
            Background="{StaticResource BackgroundLightBrush}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>           
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,0,1,0"
                    
                    Margin="10"
                    Width="100">
                <StackPanel HorizontalAlignment="Center" >
                    <RadioButton Foreground="{StaticResource TextMutedBrush}" Content="Home" Command="{Binding NavigateCommand}" CommandParameter="{Binding HomeViewModel}" IsChecked="True"/>
                    <RadioButton Foreground="{StaticResource TextMutedBrush}" Content="Products" Command="{Binding NavigateCommand}" CommandParameter="{Binding ProductsViewModel}"/>
                </StackPanel>
            </Border>

            <ContentControl Grid.Column="1" Content="{Binding CurrentViewModel}" />
        </Grid>
        </Border>
</UserControl>
